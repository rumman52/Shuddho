{
  "name": "@shuddho/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "npm run build && node --watch dist/server.js",
    "build": "npm run build --workspace @shuddho/config && npm run build --workspace @shuddho/observability && npm run build --workspace @shuddho/shared && npm run build --workspace @shuddho/nlp && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node test/check-api.test.mjs"
  },
  "dependencies": {
    "@shuddho/config": "0.1.0",
    "@shuddho/nlp": "0.1.0",
    "@shuddho/observability": "0.1.0",
    "@shuddho/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
