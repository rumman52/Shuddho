# Corrector

This package contains the future custom sequence-to-sequence corrector.

Current scope:
- encoder-decoder scaffold in PyTorch
- config-driven training placeholder
- explicit separation between training and inference code

