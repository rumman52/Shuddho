# Ranking

This module is reserved for future learned suggestion ranking.

The current MVP uses a conservative service-side merger in `services/suggestion_manager`.
Later work here should learn ranking features from feedback logs and offline datasets without depending on external models.

