# Synthetic Datasets

This folder holds synthetic error pairs created from our own clean Bangla text.

Use cases:
- detector bootstrapping
- corrector bootstrapping
- offline evaluation before real annotated datasets exist

The included sample files are placeholders and should be regenerated with `scripts/generate_synthetic_errors.py`.

